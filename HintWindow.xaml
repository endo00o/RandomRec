<Window x:Class="RandomRec.HintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Random Recorder"
        Width="400" Height="110"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">

    <Border Margin="8" RenderTransformOrigin="0.5,0.5">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.7" BlurRadius="22" ShadowDepth="0"/>
        </Border.Effect>

        <Border.RenderTransform>
            <ScaleTransform x:Name="AppearScale" ScaleX="0.85" ScaleY="0.85"/>
        </Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetName="AppearScale"
                                         Storyboard.TargetProperty="ScaleX"
                                         From="0.85" To="1" Duration="0:0:0.45">
                            <DoubleAnimation.EasingFunction>
                                <BackEase EasingMode="EaseOut" Amplitude="0.6"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetName="AppearScale"
                                         Storyboard.TargetProperty="ScaleY"
                                         From="0.85" To="1" Duration="0:0:0.45">
                            <DoubleAnimation.EasingFunction>
                                <BackEase EasingMode="EaseOut" Amplitude="0.6"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <!-- Чёрная подложка -->
            <Border Background="#0E0E0E" CornerRadius="10"/>
            <!-- Оранжевая "рябь от руки" — набросочная рамка из мелких штрихов -->
            <Rectangle Stroke="#CC785C"
                       StrokeThickness="2"
                       StrokeDashArray="1.5 2"
                       StrokeDashCap="Round"
                       RadiusX="10" RadiusY="10"
                       Margin="2"/>
            <TextBlock x:Name="HintText"
                       Text=""
                       FontFamily="Times New Roman"
                       FontSize="18"
                       Foreground="#F3ECE3"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="20,12"/>
        </Grid>
    </Border>
</Window>
